# ASMlab v0.5.0 evidence

`release-summary.json` records the current local Linux x86-64 gate: **225,984 assertion executions, zero failures**. `guard-summary.json` records a separate **41 checks, zero failures**. Counts include repeated profile/backend corpora and ABI assertions, not unique mathematical cases.

Current folders: `native/`, `runtime/`, `l3/`, `dynamic/`, `workbench/`. Reports were copied from the completed build/evidence gate; example captures were generated by the same delivered binary. `workbench/workbench-*.txt` are actual controlling-PTY cell captures, not invented layouts. The PNG in docs is rendered from the 120x34 text.

`baseline-*` folders are historical reports and source comparisons; they do not certify the current executable. `build-test.log` contains the native rebuild and subsequent complete gate; `full-release-gate.log` is the gate alone. `guard-tests.log` is the separate final guard run.

`bin/*.build.json` records source/tool/output identities. Source build IDs in Trace v2 identify input digest/profile/backend, not the ELF digest. ELF hashes are separate. These are integrity records, not signed attestations.

No remote GitHub Actions run, WSL device, ARM/Pi board, web service, graphical plotter, formal correctness or comprehensive security certification is claimed. L3 and dynamic empty-root checks were actually executed, not skipped, in this local run.
